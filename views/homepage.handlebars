<h1 style="text-align: center; margin-top: 2%">BLOGS</h1>
<!-- Loop over each object in the projects array -->
{{#each blogs as |blog| }}
<div class="row mb-4 blog">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{!-- {{{get_emoji}}} --}}
      <a class="blogTitles" href="/blogs/{{id}}"><u>{{name}}</u></a>
    </h2>
    {{!-- <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p> --}}
    <p style="text-align: center; color: white;">Created by {{user.name}} on {{format_date date_created}}</p>
  </div>
  {{!-- <div class="col-md-7">
    <p>
      {{text}}
    </p>
  </div> --}}
</div>
{{/each}}